jobs:
  include:
    - stage: test
      language: node_js
      node_js: '8.10.0'
      script:
        - yarn install --audit
        - yarn test
        - yarn build

stages:
  - test

branches:
  except:
    - /^v\d+\.\d+\.\d+$/

cache:
  directories:
    - web/.next
